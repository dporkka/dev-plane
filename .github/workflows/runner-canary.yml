name: Runner Canary

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/runner-canary.yml"
  workflow_dispatch:

jobs:
  ubuntu-24:
    name: Ubuntu 24.04 runner canary
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Confirm runner allocation
        run: echo "GitHub-hosted runner allocated successfully"
